apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: development
spec:
  selector:
    label: web
  ports:
    - name: http
      protocol: TCP
      port: 80
    - name: https
      protocol: TCP
      port: 443


